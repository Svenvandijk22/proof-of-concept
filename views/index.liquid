{% render 'partials/head.liquid' %}



<div class="zoom-overlay">
  <div class="zoom-modal">
    <button class="zoom-close" aria-label="Close zoom">✕</button>
    <button class="zoom-arrow zoom-arrow-left" aria-label="Previous image">‹</button>

    <div class="zoom-image-wrapper">
      <img class="zoom-image" src="" alt="">
    </div>

    <button class="zoom-arrow zoom-arrow-right" aria-label="Next image">›</button>

    <div class="zoom-controls">
      <button class="zoom-out" aria-label="Zoom out">−</button>
      <span class="zoom-level">100%</span>
      <button class="zoom-in" aria-label="Zoom in">+</button>
    </div>
  </div>

  <button class="zoom-cart-button">Add to basket</button>
</div>

<section class="product-layout">

 <section class="product-gallery">
  {% assign firstImage = product.images.first %}

  <div class="gallery-main">
    <button class="gallery-zoom-button" type="button">
      <img src="/assets/zoom_icon.png" alt="">
    </button>

    <img
      class="gallery-main-image"
      src="https://fdnd-agency.directus.app/assets/{{ firstImage.directus_files_id }}?width=1200"
      alt="{{ product.title }}"
      fetchpriority="high"
    >
  </div>

  <ul class="gallery-thumbnails">
    {% for image in product.images %}
      <li>
        <a
          class="gallery-thumbnail {% if forloop.first %}active{% endif %}"
          href="https://fdnd-agency.directus.app/assets/{{ image.directus_files_id }}"
        >
          <img
            src="https://fdnd-agency.directus.app/assets/{{ image.directus_files_id }}?width=300"
            alt="{{ product.title }}"
            loading="lazy"
          >
        </a>
      </li>
    {% endfor %}
  </ul>
</section>

  <section class="product-details">
    <p class="product-brand">{{ product.brand }}</p>
    <h1 class="product-title">{{ product.title }} - White</h1>

    <div class="product-rating">
      <span>★★★★★</span>
      <span>5</span>
      <a href="#reviews">5 reviews</a>
    </div>

    <p class="product-summary">{{ product.summary }}</p>
    <p class="product-id">ID {{ product.id }}</p>

    <div class="product-colour">
      <p>Colour <span>cotton white</span></p>

      <div class="colour-options">
        {% for image in images limit: 2 %}
          <button class="colour-option {% if forloop.first %}active{% endif %}">
            <img src="{{ image }}" alt="{{ product.title }}">
          </button>
        {% endfor %}
      </div>
    </div>

    <form class="product-form">
      <div class="size-header">
        <label for="size">Size</label>
        <a href="">Size guide</a>
      </div>

      <select id="size" name="size">
        <option value="" disabled selected hidden>Select</option>

        {% for size in product.sizes %}
          {% if size.available %}
            <option value="EU {{ size.size }}">EU {{ size.size }}</option>
          {% else %}
            <option value="EU {{ size.size }}" disabled>
              EU {{ size.size }} - Sold out
            </option>
          {% endif %}
        {% endfor %}
      </select>

      <p class="product-price">€{{ product.price }}</p>
      <p class="payment-text">Or €14.99/month with payment plans</p>
      <a class="shipping-link" href="#">Shipping costs may apply</a>

      <button type="submit" class="add-to-cart-button">
        Add to basket
      </button>
    </form>

    <p class="seller">
      Sold and shipped by <strong>DECATHLON</strong>
    </p>

    <ul class="product-services">
      <li>365 days to return for members <span>+</span></li>
      <li>2-year warranty <span>+</span></li>
    </ul>

    <section class="delivery-options">
      <h2>How to get it</h2>

      <p><strong>Home delivery</strong> <a href="">Enter postcode</a></p>
      <p class="available"> Get it as soon as 2 working days</p>

      <p><strong>Pick up at Decathlon</strong> <a href="#">Check Availability</a></p>

      <p><strong>Pick up near you</strong></p>
      <p class="available"> Get it as soon as 2 working days</p>
    </section>
  </section>

</section>

<section class="reviews-section" id="reviews">

  <form action="/reviews" method="post" class="review-form">
    <label for="review-title">
      Write a review
    </label>

    <input
      type="text"
      id="review-title"
      name="review-title"
      required
    >

    <button type="submit" class="review-submit-button">
      <span class="button-text">Send review</span>
      <span class="button-loader"></span>
    </button>
  </form>

  <div class="review-success-overlay">
    <section class="review-success-popup">
      <button class="success-close" type="button">×</button>

      <div class="success-icon">
        <img
          src="/assets/succes icon.png"
          alt="Review submitted successfully"
        >
      </div>

      <h2>Review submitted!</h2>

      <p>
        Thank you for sharing your experience.
        Your review helps other customers make the right choice.
      </p>
    </section>
  </div>

  {% for review in reviews %}
    <article class="review-card">
      <h3>{{ review.title }}</h3>
      <p>{{ review.description }}</p>
      <p>{{ review.name }}</p>

      <form action="/reviews/delete" method="post">
        <input
          type="hidden"
          name="id"
          value="{{ review.id }}"
        >

        <button type="submit">
          Delete review
        </button>
      </form>
    </article>
  {% endfor %}

</section>


</body>

{% render 'partials/foot.liquid' %}